{
  "name": "insideorgs",
  "version": "1.0.0",
  "scripts": {
    "vercel-build": "python -m pip install -r requirements.txt && python manage.py collectstatic --noinput"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 